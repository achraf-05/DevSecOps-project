apiVersion: v1
kind: Service
metadata:
  name: devsecops-api-svc
  namespace: default
  labels:
    app: devsecops-api
spec:
  type: ClusterIP
  selector:
    app: devsecops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
